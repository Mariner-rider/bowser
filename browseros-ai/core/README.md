# core

BrowserOS integration layer with strict extension boundaries.

- `chromium_bridge/`: adapter contracts to BrowserOS/Chromium lifecycle hooks.
- `networking/`: outbound request mediation and policy checks.
- `security/`: runtime isolation, permissions, and secrets boundaries.
