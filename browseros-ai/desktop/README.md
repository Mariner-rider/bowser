# BrowserOS AI Desktop App

Desktop distribution scaffold using Electron runtime wrappers.

## Run (after installing electron in host app)
- `npm run desktop:dev`

## Build artifacts
- `npm run build-desktop`

This scaffold keeps the desktop shell separate from browser runtime core.
